describe('LapService', function () {

  var LapService;

  beforeEach(function () {
    module('the-map');
    inject(function (_LapService_) {
      LapService = _LapService_;
    });
  });

  xit('should have tests', function () {
    //expect(LapService.doSomething()).to.equal('something');
  });

});