describe('LineService', function () {

  var LineService;

  beforeEach(function () {
    module('the-map');
    inject(function (_LineService_) {
      LineService = _LineService_;
    });
  });

  xit('should have tests', function () {
    //expect(LineService.doSomething()).to.equal('something');
  });

});